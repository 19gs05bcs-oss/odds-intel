Bu klasör GitHub'a yüklemek / kopyalamak içindir (hesap askıdayken).

1) Supabase SQL Editor → SCHEMA_CALISTIR.sql (veya migrate_missing_tables.sql) çalıştır
2) Bu klasörün içeriğini GitHub repo root'una kopyala / push et
3) Actions secrets: SUPABASE_URL, SUPABASE_KEY, BWIN_ACCESS_ID

Yeni model: oranlar events.markets_json (tek satır JSON), history = event_odds_history
